default: &default
  adapter: "mysql2"
  encoding: "utf8"
  username: root
  password:
  timeout: 5000

development:
  <<: *default
  database: rails
  host: mysql

test:
  <<: *default
  database: rails-test
  host: mysql
